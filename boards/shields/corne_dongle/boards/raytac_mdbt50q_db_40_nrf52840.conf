# Use the code partition from the DTS (required for Nordic nRF5 SDK DFU bootloader)
CONFIG_USE_DT_CODE_PARTITION=y

# MDBT50Q-CX-40 dongle uses LDO only (no DC-DC inductors on board)
CONFIG_BOARD_ENABLE_DCDC=n
CONFIG_BOARD_ENABLE_DCDC_HV=n
